# Template — replace placeholder values before applying.
# Encode values: echo -n 'your-value' | base64
#
# Do NOT commit this file with real secrets.
# Use sealed-secrets, external-secrets, or a secrets manager in production.
apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: dverse
type: Opaque
data:
  SECRET_KEY: REPLACE_WITH_BASE64_SECRET_KEY
  ANTHROPIC_API_KEY: REPLACE_WITH_BASE64_ANTHROPIC_API_KEY
  LOGFIRE_TOKEN: REPLACE_WITH_BASE64_LOGFIRE_TOKEN
